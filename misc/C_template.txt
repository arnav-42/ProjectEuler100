#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <time.h>

int main(void) {

    clock_t start = clock();




    clock_t finish = clock();


    double cpu_time_used = ((double) (finish - start)) / CLOCKS_PER_SEC;
    printf("Time: %f seconds\n", cpu_time_used);

    return 0;

}
